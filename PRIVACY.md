# 隐私说明

最后更新：2026-08-04

## 产品处理的数据

秋招填表助手处理用户主动填写的求职档案，包括联系方式、教育经历、实习经历、项目经历、求职偏好和常用回答。用户扫描招聘网页时，扩展还会处理当前页面中的表单标签、ARIA 文本、占位符、字段名、控件类型和有限的附近上下文。

用户也可以在求职档案页主动选择一份 PDF 或 DOCX 简历。扩展在本机浏览器内提取文字；遇到异常文字层时，可使用随扩展打包的本地 OCR 资源，然后通过确定性规则把有依据的信息填入现有档案字段。无法识别的内容保持空白；身份证号等不在档案模型中的敏感信息不会因简历导入而保存。

扩展不会在扫描过程中读取网页输入框已有的值。

## 数据保存位置

当前版本没有产品服务器、云端账号、遥测或第三方分析服务。求职档案、隐私确认状态和网站字段对应关系默认保存在用户设备上的浏览器扩展存储中。

所选简历文件、文件路径和提取出的完整原文不会写入扩展存储。解析结果先以未保存状态显示在原有信息表中；只有用户检查后点击“保存档案”，对应的结构化字段才会存入本机。

用户可以在“本地数据管理”中：

- 导出一份 JSON 备份；
- 导入备份并替换现有档案和映射；
- 永久删除档案和全部网站字段映射。

导出的文件由用户自行选择保存位置和保管方式。

## 页面访问

扩展使用 `activeTab` 权限。只有用户点击扩展并主动扫描时，扩展才获得当前标签页的临时访问权。当前版本不申请永久的全网站访问权限，也不记录浏览历史。

## 明确排除的数据与行为

- 不保存招聘网站密码、登录 Cookie 或身份验证令牌；
- 不填写密码、验证码、身份证号、护照号、银行卡号或文件选择控件；
- 不绕过短信验证、图片验证或身份认证；
- 不点击最终提交申请的按钮；
- 不向外部服务器发送求职档案或页面表单信息。
- 不向外部服务器发送上传的简历文件或提取出的简历全文；当前解析不调用云端 AI、云端 OCR 或语义服务，本地 OCR 资源随扩展安装并在设备上运行。

出生日期、性别、籍贯和政治面貌等字段即使匹配成功，也默认要求用户手动确认。

## 用户责任与产品边界

用户应在提交申请前检查所有填写内容，并确保其真实、准确、符合招聘网站要求。招聘网站可能随时修改页面结构；扩展无法保证所有字段和控件都能被识别或填写。

如果未来版本增加云同步、AI 服务或遥测，本说明和产品内显著告知必须在功能启用前更新，并重新获得用户明确同意。
