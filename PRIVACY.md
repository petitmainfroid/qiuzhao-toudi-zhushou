# 隐私说明

最后更新：2026-08-06

## 产品处理的数据

秋招填表助手处理用户主动填写的求职档案，包括联系方式、教育经历、实习经历、项目经历、求职偏好和常用回答。用户扫描招聘网页时，扩展还会处理当前页面中的表单标签、ARIA 文本、占位符、字段名、控件类型和有限的附近上下文。

用户也可以在求职档案页主动选择一份 PDF 或 DOCX 简历。扩展在本机浏览器内提取文字；遇到异常文字层时，可使用随扩展打包的本地 OCR 资源，然后通过确定性规则把有依据的信息填入现有档案字段。无法识别的内容保持空白；身份证号等不在档案模型中的敏感信息不会因简历导入而保存。

扩展不会在扫描过程中读取网页输入框已有的值。

## 数据保存位置

当前版本没有产品服务器、云端账号、遥测或第三方分析服务。求职档案、隐私确认状态和网站字段对应关系默认保存在用户设备上的浏览器扩展存储中。用户主动选择的最近一份 PDF 简历会保存在扩展的本地 IndexedDB 中，记录包括文件字节、文件名、MIME 类型、大小、SHA-256 摘要和保存时间；选择新 PDF 会替换旧文件。

扩展不会保存文件系统路径。DOCX 原件和提取出的完整原文不会写入扩展存储。PDF 原件会按上段说明保存，以便后续投递复用；解析结果仍先以未保存状态显示在原有信息表中，只有用户检查后点击“保存档案”，对应的结构化字段才会存入本机。

在招聘页面附加简历是另一条独立流程。扩展会读取并重新校验本机保存的 PDF，显示文件名、大小、摘要前缀、目标 HTTPS 站点和匹配的简历控件。只有用户再次确认，扩展才把这一个文件交给该页面的控件；招聘网站可能随即把文件传到其服务器。扩展不保存文件路径，也不把文件发送到本产品自己的服务器。

用户可以在“本地数据管理”中：

- 导出一份 JSON 备份；
- 导入备份并替换现有档案和映射；
- 单独删除保存的 PDF 简历；
- 永久删除档案、全部网站字段映射和保存的 PDF 简历。

JSON 导出不包含 PDF 字节或提取全文。导出的文件由用户自行选择保存位置和保管方式。

## 页面访问

为提供无需另装 OpenCLI 的浏览器操作内核，扩展声明 `debugger`、`tabs`、`webNavigation`、`alarms` 和 `<all_urls>` 权限，并保留 `activeTab`、`scripting`、`sidePanel`、`storage`。这会让 Chrome/Edge 在安装时显示较强的网页访问警告。

这些权限不代表扩展会在后台任意读取网页。只有用户在扩展界面点击“连接当前招聘页”、扫描、填写或单独确认附件后，扩展才处理目标页面。会话只允许没有内嵌用户名/密码的 HTTPS 地址，固定一个 Origin，最长 10 分钟；跨 Origin、标签页关闭、到期或调试器断开时会停止。会话状态只保存 sessionId、标签页编号、Origin、路径、起止时间、可交互控件数、frame 数和失败类别，不保存页面输入值或浏览历史。

内核写入还要求用户单独点击一次授权。该授权最长 60 秒，只绑定当前会话、Origin 和本地档案版本；写入值由扩展在安全检查后从本地档案白名单中解析，调用方不能传入任意值、选择器或脚本。每次动作在页面内回读为布尔结果，公开响应不含档案路径、请求值、旧值或当前值；过期引用、节点替换、语义变化、禁用/只读、跨 Origin 和回读失败都会停止。自动降级最多一次且不使用剪贴板。

内核不公开任意 JavaScript/CDP 执行接口，不读取 Cookie、Authorization、网络请求/响应正文、密码或验证码。第三方代码与许可证见 `THIRD_PARTY_NOTICES.md` 和 `third_party/opencli/LICENSE`。

## 明确排除的数据与行为

- 不保存招聘网站密码、登录 Cookie 或身份验证令牌；
- 不填写密码、验证码、身份证号、护照号、银行卡号或任意文件选择控件；唯一例外是用户保存或临时亲选、核对并再次确认的单个 PDF 简历，且只能交给唯一、高置信度的简历控件；
- 不绕过短信验证、图片验证或身份认证；
- 不点击最终提交申请的按钮；
- 不向外部服务器发送求职档案或页面表单信息。
- 不把简历文件或提取全文发送给本产品服务器、云端 AI、云端 OCR 或语义服务；本地 OCR 资源随扩展安装并在设备上运行。用户确认招聘页面附件后，目标招聘网站可能立即接收该 PDF，这一目的地会在确认按钮前明确显示。

出生日期、性别、籍贯和政治面貌等字段即使匹配成功，也默认要求用户手动确认。

## 用户责任与产品边界

用户应在提交申请前检查所有填写内容，并确保其真实、准确、符合招聘网站要求。招聘网站可能随时修改页面结构；扩展无法保证所有字段和控件都能被识别或填写。

如果未来版本增加云同步、AI 服务或遥测，本说明和产品内显著告知必须在功能启用前更新，并重新获得用户明确同意。
