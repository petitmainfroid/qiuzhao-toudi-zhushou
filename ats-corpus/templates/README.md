# ATS mapping templates

This directory is reserved for reviewed family-level mappings from anonymous controls to canonical profile fields and fixed behavior drivers. Templates must never contain profile values, credentials, site-session data, or a final-submit action.
