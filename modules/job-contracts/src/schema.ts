import { JOB_ACTIONS, JOB_SOURCES, JOB_STATES, TERMINAL_REASONS } from "./types";
const closed = (properties: Record<string, unknown>, required: readonly string[]) => ({ type: "object", additionalProperties: false, properties, required } as const);
const id = { type: "string", pattern: "^[A-Za-z0-9][A-Za-z0-9:_-]{0,127}$" } as const;
const text = (maxLength: number) => ({ type: "string", minLength: 1, maxLength } as const);
const dateTime = { type: "string", format: "date-time", maxLength: 64 } as const;
const safeOrigin = { type: "string", pattern: "^https://[A-Za-z0-9.-]+(?::[0-9]{1,5})?$", maxLength: 512 } as const;
const safePath = { type: "string", pattern: "^/(?:[A-Za-z0-9._~!$&'()*+,;=:@%/-]*)$", maxLength: 2048 } as const;
const profilePath = { type: "string", pattern: "^[A-Za-z][A-Za-z0-9_]*(?:\\.(?:[A-Za-z][A-Za-z0-9_]*|0|[1-9][0-9]*))*$", maxLength: 256 } as const;
export const JOB_IDENTITY_SCHEMA = Object.freeze(closed({ source: { enum: JOB_SOURCES }, origin: safeOrigin, path: safePath, sourceJobRef: id }, ["source", "origin", "path", "sourceJobRef"]));
export const JOB_RECORD_SCHEMA = Object.freeze(closed({ schemaVersion: { const: 1 }, jobId: id, identity: JOB_IDENTITY_SCHEMA, title: text(256), company: text(256), location: text(256), description: text(20000), state: { enum: JOB_STATES }, version: { type: "integer", minimum: 0, maximum: 2147483647 }, createdAt: dateTime, updatedAt: dateTime }, ["schemaVersion", "jobId", "identity", "title", "company", "location", "description", "state", "version", "createdAt", "updatedAt"]));
export const JOB_EVENT_SCHEMA = Object.freeze(closed({ schemaVersion: { const: 1 }, eventId: id, jobId: id, kind: { enum: ["discovered", "prefiltered", "scored", "review_required", "message_planned", "message_sent", "reply_detected", "blocked", "cancelled"] }, at: dateTime, requestId: id, terminalReason: { enum: TERMINAL_REASONS } }, ["schemaVersion", "eventId", "jobId", "kind", "at", "requestId"]));
export const JOB_RANKING_REQUEST_SCHEMA = Object.freeze(closed({ schemaVersion: { const: 1 }, job: closed({ jobId: id, title: text(256), company: text(256), location: text(256), description: text(20000) }, ["jobId", "title", "company", "location", "description"]), profileCatalog: { type: "array", maxItems: 4096, items: closed({ path: profilePath, hasValue: { type: "boolean" }, safetyClass: { enum: ["ordinary", "sensitive"] } }, ["path", "hasValue", "safetyClass"]) } }, ["schemaVersion", "job", "profileCatalog"]));
export const JOB_RANKING_DECISION_SCHEMA = Object.freeze(closed({ schemaVersion: { const: 1 }, jobId: id, outcome: { enum: ["pass", "reject", "review"] }, reason: { enum: ["relevant", "not_relevant", "insufficient_profile", "ambiguous"] } }, ["schemaVersion", "jobId", "outcome", "reason"]));
export const JOB_ACTION_LEASE_SCHEMA = Object.freeze(closed({ schemaVersion: { const: 1 }, leaseId: id, jobIds: { type: "array", minItems: 1, maxItems: 100, uniqueItems: true, items: id }, origins: { type: "array", minItems: 1, maxItems: 16, uniqueItems: true, items: safeOrigin }, actions: { type: "array", minItems: 1, maxItems: 2, uniqueItems: true, items: { enum: JOB_ACTIONS } }, expiresAt: dateTime }, ["schemaVersion", "leaseId", "jobIds", "origins", "actions", "expiresAt"]));
export const CONVERSATION_INTENT_SCHEMA = Object.freeze(closed({ schemaVersion: { const: 1 }, jobId: id, kind: { enum: ["generate_greeting", "manual", "review"] }, reason: { enum: TERMINAL_REASONS } }, ["schemaVersion", "jobId", "kind"]));
export const CONVERSATION_EXECUTION_SCHEMA = Object.freeze(closed({ schemaVersion: { const: 1 }, planId: id, requestId: id }, ["schemaVersion", "planId", "requestId"]));
