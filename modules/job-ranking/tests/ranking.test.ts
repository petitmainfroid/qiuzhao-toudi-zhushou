import{describe,it,expect,vi}from"vitest";import{prefilter,rankWithAi}from"../src/index";const x={schemaVersion:1,job:{jobId:"job:1",title:"Engineer",company:"Example",location:"Shanghai",description:"TypeScript local tools"},profileCatalog:[{path:"basics.fullName",hasValue:true,safetyClass:"ordinary"}]};describe("ranking",()=>it("runs deterministic gate before closed AI decision",async()=>{expect(prefilter(x,{requiredTerms:["typescript"],excludedTerms:[]})).toMatchObject({outcome:"pass"});expect(prefilter(x,{requiredTerms:[],excludedTerms:["sales"]})).toMatchObject({outcome:"pass"});const ai=vi.fn(async()=>({schemaVersion:1,jobId:"job:1",outcome:"review",reason:"ambiguous"}));expect(await rankWithAi(x,{requiredTerms:["typescript"],excludedTerms:[]},ai)).toMatchObject({outcome:"review"});expect(ai).toHaveBeenCalledOnce();expect(await rankWithAi(x,{requiredTerms:["python"],excludedTerms:[]},ai)).toMatchObject({outcome:"review"});expect(ai).toHaveBeenCalledOnce()}));
