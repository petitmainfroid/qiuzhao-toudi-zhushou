import {describe,it,expect} from "vitest";import{DurableJobWorkflow,JobWorkflowError}from"../src/index";
describe("durable job workflow",()=>it("locks, budgets, restarts and cancels fail closed",()=>{const w=new DurableJobWorkflow();const start=w.start("job:1","request:1");expect(()=>w.start("job:1","request:2")).toThrow(/locked/);w.consume("job:1",start.epoch);w.consume("job:1",start.epoch);expect(()=>w.consume("job:1",start.epoch)).toThrow(/budget_exhausted/);w.restart();expect(w.snapshot("job:1")).toMatchObject({state:"needs_replan"});const next=w.start("job:1","request:3");w.cancel("job:1");expect(()=>w.complete("job:1",next.epoch)).toThrow(JobWorkflowError)}));
